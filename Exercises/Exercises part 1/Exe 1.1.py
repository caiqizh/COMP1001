def main():
    t = eval(input('Enter number of rows: '))
    need = 1
    list = []
    for row in range(t):
        list = []
        for num in range(row):
            order = 0
            if order<=i:
                list.append(need)
                order = order + 1
                need = need + 1
            print(list)
            
        
